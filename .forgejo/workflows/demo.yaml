name: Build GitBuilding Docs

on: [push]
jobs:

  test:
    runs-on: [codeberg-tiny, codeberg-tiny-lazy]
    steps:
      - uses: actions/checkout@v4

    - name: Install GitBuilding
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade gitbuilding    
